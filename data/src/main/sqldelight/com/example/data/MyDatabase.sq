CREATE TABLE TaskDB (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  description TEXT,
  date TEXT
);

insertTask:
INSERT INTO TaskDB (title, description, date)
VALUES (?, ?,?);

getAllTasks:
SELECT *
FROM TaskDB;

getTaskById:
SELECT *
FROM TaskDB
WHERE id = ?;
